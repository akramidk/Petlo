import { Text } from "react-native";
import Scrollable from "../_Scrollable";

const Home = () => {
  return (
    <Scrollable>
      <Text>Home</Text>
    </Scrollable>
  );
};

export default Home;
